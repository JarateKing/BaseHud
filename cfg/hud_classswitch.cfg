// if setup properly
// this cfg should get executed on any class switch
// this can also be used as a method to run scripts whenever the player enters ingame

// trigger animations with MenuOpen and MenuClose if need be (such as animation locks)
anim_lock